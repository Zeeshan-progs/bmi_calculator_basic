import 'package:flutter/material.dart';

const label = TextStyle(
  fontSize: 25,
  color: Color(0xFF8D8E98),
);

final containerColor = Color(0xFF1D1E33);
const buttonColor = Colors.pink;

const buttonStyle = TextStyle(fontSize: 50, color: Colors.white, shadows: [
  BoxShadow(
    color: Colors.black,
    blurRadius: 10,
  ),
]);

final double minValue = 0.0;
final double maxValue = 150.0;

final inActiveColor = Color(0xFF1D1E33);
final activeColor = Colors.pink;
double inActiveHeight = 180;
double inActiveWidth = 180;
double activeHeight = 200;
double activeWidth = 200;
